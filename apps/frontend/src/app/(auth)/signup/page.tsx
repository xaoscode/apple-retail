import { Metadata } from "next";

export const metadata: Metadata = {
    title: 'registration'
}

export default function Signup() {
    return (
        <div >
            О нас
        </div>
    );
}
